import { Wame } from './Wame';

/**
 * Backward-compatible alias for {@link Wame}. Behaves identically.
 *
 * @deprecated Use `Wame` instead — `new Wame(init)`.
 */
export class WhatsApp extends Wame {}

export default WhatsApp;
